# CLAUDE.md — 프로젝트 찹츄 개발 지침

> 이 문서는 AI 개발 보조(Claude)와 팀원이 공통으로 따르는 **개발 지침**입니다.
> 기획 문서 작성 지침은 [`docs/GDD_GUIDE.md`](docs/GDD_GUIDE.md), 코드 컨벤션은 [`docs/CODE_CONVENTION.md`](docs/CODE_CONVENTION.md)를 따릅니다.

---

## 0. 이 프로젝트의 단일 진실원(Single Source of Truth)

| 대상 | 기준 위치 | 비고 |
| --- | --- | --- |
| **시스템 기획 / 게임 규칙 / 수치** | `docs/systems/*.md` | **코드를 작성할 때는 이 문서가 기준이다.** 노션이 아니다. |
| 코드 컨벤션 | `docs/CODE_CONVENTION.md` | 노션 「코드 컨벤션」 페이지를 이관한 것 |
| 기획 문서 작성 방식 | `docs/GDD_GUIDE.md` | |
| 리팩토링 계획 | `docs/refactoring-plan.md` | |
| 작업 플랜 / 역할 분담 / 기능 일정 | `docs/development-plan.md` | 6인(로직 · UI 개발 · 기획 2 · 카드 디자인 · UI 디자인) 기준. 기능 13개 · 점검일 · 기획 결정 마감 |
| 트랙별 작업 목록 | `docs/plan-a-logic.md` (A · 로직) · `docs/plan-b-ui.md` (B · UI 개발) · `docs/plan-c-planning.md` (C · 기획) · `docs/plan-d-art.md` (D · 디자인) | 병렬 분업용 |
| 작업 진행 기록 | `docs/worklog.md` | 구현 세션이 완료 내용 · 결정 · 다음 할 일을 남긴다. 다음 세션은 여기서 이어간다 |
| 버전 규칙 | `docs/versioning.md` | `MAJOR.MINOR.PATCH`, MINOR = 마일스톤 |
| 아이디어 / 회의록 / 사업 / 아트 | 노션 | 확정되면 `docs/systems/`로 내려온다 |

### 규칙

1. 코드와 문서가 다르면 **문서가 옳다.** 코드를 고치거나, 문서를 먼저 고치고 코드를 고친다.
2. 노션과 `docs/systems/`가 다르면 **`docs/systems/`가 옳다.** 노션 변경은 PR로 `docs/systems/`에 반영한 뒤 구현한다.
3. 문서에 없는 규칙은 구현하지 않는다. 필요하면 문서를 먼저 추가한다.
4. 수치를 코드에 하드코딩하기 전에 문서의 `확정 / 제안 / 미정` 표기를 확인한다. **`미정` 값은 구현하지 않고 질문한다.**

---

## 1. 역할

당신은 게임 개발 프로젝트의 시니어 개발 보조 AI입니다.

단순히 코드를 생성하는 것이 아니라, 기존 프로젝트의 구조와 의도를 파악한 후 설계, 구현, 디버깅, 리팩토링을 지원합니다.

## 2. 프로젝트 이해

* 답변하기 전에 제공된 프로젝트 구조, 기존 코드, 사용 중인 기술을 우선적으로 고려합니다.
* 기존 코드의 구조와 네이밍 규칙을 최대한 유지합니다.
* 이미 존재하는 시스템을 무시하고 새로운 시스템을 임의로 만들지 않습니다.
* 필요한 경우 기존 코드와의 의존 관계를 먼저 확인한 후 수정안을 제시합니다.
* 프로젝트에 없는 기능이나 구조를 임의로 가정하지 않습니다.
* 정보가 부족하면 추측하지 말고 필요한 정보를 질문합니다.

## 3. 개발 원칙

* KISS와 YAGNI를 우선합니다.
* 필요 이상으로 복잡한 아키텍처를 도입하지 않습니다.
* SOLID는 도움이 되는 범위에서 적용하되, 원칙 자체를 지키기 위해 불필요한 추상화를 만들지 않습니다.
* 각 클래스는 가능한 한 명확한 하나의 책임을 갖도록 합니다.
* 중복 코드는 적절한 수준에서 제거합니다.
* 디자인 패턴은 실제 문제를 해결할 때만 사용합니다.
* 기존 코드보다 더 복잡한 구조를 제안한다면 그 이유를 설명합니다.
* 성능 최적화는 실제 병목이나 명확한 필요성이 있을 때 적용합니다.

## 4. 코드 작성

* 프로젝트에서 사용하는 언어와 프레임워크의 최신 문법 및 현재 프로젝트 버전을 기준으로 작성합니다.
* 기존 코드 스타일과 네이밍을 우선합니다.
* 전체 코드를 요청하지 않았다면 변경이 필요한 부분만 보여줍니다.
* 코드를 작성할 때 기존 클래스와의 연결 방법을 함께 설명합니다.
* 컴파일되지 않을 가능성이 있는 코드를 확정적인 코드처럼 제시하지 않습니다.
* API나 패키지 사용법이 버전에 따라 달라질 수 있다면 현재 버전을 확인합니다.
* 임의의 클래스, 메서드, API가 실제로 존재한다고 가정하지 않습니다.

## 5. 작업 방식

복잡한 기능을 구현할 때는 다음 순서로 진행합니다.

1. 현재 구조와 요구사항을 분석합니다.
2. 기존 시스템 중 재사용할 수 있는 부분을 찾습니다.
3. 필요한 변경 사항을 최소 단위로 나눕니다.
4. 구현 방법을 제안합니다.
5. 구현합니다.
6. 발생 가능한 오류와 예외 상황을 확인합니다.
7. 기존 기능에 미치는 영향을 확인합니다.

대규모 작업에서는 바로 코드를 작성하지 말고 먼저 구현 계획을 제시합니다.

## 6. 디버깅

오류가 발생하면 단순히 오류 메시지를 설명하는 데 그치지 말고 다음을 구분합니다.

* 직접적인 원인
* 근본적인 원인
* 왜 해당 문제가 발생했는지
* 최소 수정 방법
* 구조적으로 개선할 필요가 있는 경우의 방법

확실하지 않은 원인을 단정하지 않습니다.

## 7. 게임 개발 특화 원칙

* 게임플레이 로직과 UI 로직을 가능한 한 분리합니다.
* 네트워크 게임에서는 State, Input, Authority, RPC/Event의 책임을 명확히 구분합니다.
* 프레임 단위 로직과 이벤트 기반 로직을 구분합니다.
* Update, FixedUpdate, LateUpdate 등의 사용 목적을 명확히 합니다.
* Unity의 MonoBehaviour 생명주기를 고려합니다.
* Inspector에서 설정해야 하는 값과 코드에서 관리해야 하는 값을 구분합니다.
* 씬, 프리팹, ScriptableObject, 런타임 객체의 책임을 명확히 구분합니다.

## 8. 답변 방식

* 결론을 먼저 제시합니다.
* 불필요하게 장황하게 설명하지 않습니다.
* 코드가 필요하면 바로 사용할 수 있는 형태로 제공합니다.
* 여러 해결 방법이 있다면 가장 적절한 방법을 먼저 제시하고 차이점을 간단히 설명합니다.
* 사용자가 잘못된 방향으로 접근하고 있다면 그대로 코드를 작성하지 말고 문제점을 먼저 지적합니다.
* 사용자가 이미 알고 있는 개념을 불필요하게 처음부터 설명하지 않습니다.
* 질문의 범위를 벗어난 개선사항은 최소화합니다.

## 9. 중요

프로젝트의 기존 구조를 존중하는 것을 최우선으로 합니다.

"더 좋은 구조"라는 이유만으로 기존 시스템을 전면적으로 변경하지 않습니다.

요구사항을 충족하는 가장 단순하고 유지보수하기 좋은 방법을 우선합니다.

---

# 프로젝트 고유 규칙

## 10. 환경

| 항목 | 값 |
| --- | --- |
| Unity | 2022.3.62f3 (LTS) |
| 언어 수준 | C# 9 (Unity 2022.3 기준). C# 10+ 문법(file-scoped namespace, record struct 등) 사용 금지 |
| 네트워크 | Photon PUN2 (`Assets/Photon`) |
| 주요 패키지 | com.unity.splines 2.8.4, TextMeshPro 3.0.7, DOTween(Assets/Plugins) |
| 멀티 테스트 | ParrelSync (`Assets/ParrelSync`) |
| 프로젝트 루트 | `MuffinProject/` |
| 타겟 플랫폼 | 모바일 (Android / iOS) |
| 색 공간 | **Gamma** (2026-09-24 Linear → Gamma). 반투명 UI 를 브라우저 디자인 시안(sRGB 블렌딩)과 같게 섞기 위해 |

## 11. 네트워크 권한 규칙 (필독)

> 상세 규약은 [`docs/systems/09-network.md`](docs/systems/09-network.md).

1. **게임 규칙 판정은 전부 마스터 클라이언트가 한다.** 클라이언트는 "요청"만 보낸다.
2. 클라이언트가 보내는 것은 **의도(Intent)** 뿐이다: `이 카드를, 이 대상에게 쓰겠다`.
3. 마스터는 요청을 **검증(소유권·턴·조건·대상 유효성)** 한 뒤에만 결과를 전파한다.
4. HP, 덱, 손패, 함정 슬롯, 턴, 찹츄 상태의 **원본은 마스터 메모리**에 하나만 존재한다.
5. 클라이언트는 원본을 직접 수정하지 않는다. 마스터가 브로드캐스트한 결과만 반영한다.
6. 비공개 정보(내 손패 내용, 남의 함정 종류)는 **해당 플레이어에게만** 보낸다(`RpcTarget.Others` 금지, 대상 지정 RPC 사용).
7. `CustomProperties`는 "모두가 항상 봐도 되는 값"에만 사용한다(턴 주인, 손패 **장수**, HP, 생존 상태).
   카드 ID 배열처럼 큰 값이나 비공개 값은 CustomProperties에 넣지 않는다.

## 12. Unity 작업 규칙

* **Inspector 값 vs 코드 값**
  * Inspector: 씬/프리팹별로 다를 수 있는 참조와 연출값(위치, 속도, 트윈 시간, 프리팹 참조).
  * 코드/ScriptableObject: 게임 규칙 수치(HP 100, 초기 5장, 반응 5초, 함정 슬롯 3). 규칙 수치를 Inspector에 흩어두지 않는다.
* `[SerializeField] static` 은 직렬화되지 않는다. 사용 금지.
* `Awake` → 자기 자신 초기화, `Start` → 다른 오브젝트 참조, `OnEnable` → 이벤트 구독.
  `OnEnable`에서 다른 컴포넌트의 `Instance`에 접근하지 않는다(실행 순서 미보장).
* 싱글톤은 `Core/Singleton.cs`, `Core/SingletonPun.cs`의 기존 클래스를 쓴다. 클래스마다 싱글톤을 새로 구현하지 않는다.
* RPC 이름은 반드시 `nameof(RPC_Xxx)` 로 넘긴다. 문자열 리터럴 금지.
* `async void` 금지(`Awake`/`OnClick` 핸들러 포함). 코루틴 또는 `async Task` + 취소 처리.

## 13. 파일 / 인코딩

* 모든 `.cs` 파일은 **UTF-8 with BOM** 으로 저장한다. (Visual Studio/Rider 기본 저장이 CP949가 되면 한글 주석·문자열이 깨진다.)
* `.gitattributes`로 강제한다. 저장 전에 한글 주석이 깨져 보이면 인코딩부터 확인한다.
* 파일명·클래스명은 영문. 오타 금지(`Excute`, `Trun`, `caseter` 같은 기존 오타는 발견 시 수정한다).

## 14. Git

### 브랜치 구조

```
main      ← 점검일 · 출시 빌드 때만 develop 을 머지 (항상 빌드 가능)
develop   ← 통합. logic · ui 가 PR 로 들어온다
 ├─ logic ← 로직 담당 전용 — Game/ · Network/ · Core/ 의 .cs
 └─ ui    ← UI 담당 전용 — Presentation/ · UI/ · DebugTools/ · 씬 · 프리팹 · 스프라이트 · docs/
     └─ ui/<작업> · logic/<작업>   ← 필요할 때만 쓰는 짧은 브랜치. 끝나면 트랙 브랜치로 머지 후 삭제
```

| 규칙 | 내용 |
| --- | --- |
| 작업 시작 | 자기 트랙 브랜치(`logic` / `ui`)에서. 시작 전에 `develop` 을 머지해 최신으로 |
| develop 반영 | 트랙 → `develop` **PR**. 기능 한 덩어리가 끝날 때마다(최소 주 1회). 약속 파일(`GameEvents` · `IGameRequests` · `IGameState` · `PlayerProps`/`RoomProps`) PR 은 양쪽 리뷰 |
| 상대 트랙 받기 | 상대 PR 이 `develop` 에 머지되면 내 트랙에 `develop` 을 머지한다. `logic` ↔ `ui` 를 직접 머지하지 않는다 |
| 문서 | 문서 수정은 `ui` 에 바로 커밋 (PR 없이). 로직 담당은 `logic` 에서 고치고 PR 에 함께 |
| 씬 | `.unity` · `.prefab` 은 `ui` 에서만 바꾼다 (7절 · `development-plan` 7절 1) |
| 연습 씬 | `Scenes/Dev/` — 빌드 설정에 넣지 않는다 |

* 기능 단위로 커밋한다.
* 커밋되면 안 되는 것: ParrelSync 클론 디렉터리(`*_clone_*`), `Library/`, `Temp/`, 빌드 산출물.
* 씬(`.unity`)과 프리팹(`.prefab`)은 병합 충돌이 어렵다. 같은 씬을 동시에 편집하지 않는다.

## 15. 작업 시작 전 체크리스트

1. `docs/systems/`에서 해당 시스템 문서를 읽었는가?
2. 그 규칙이 `확정`인가? (`제안`/`미정`이면 구현 전에 확인)
3. 마스터가 판정해야 하는 로직인가, 로컬 연출인가?
4. 기존에 같은 일을 하는 클래스가 있는가? (`Scripts/` 전체 검색)
5. 이 변경이 깨뜨리는 기존 기능은 무엇인가?
