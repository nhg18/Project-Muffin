# 03. 턴 시스템

**최종 수정일**: 2026-09-22
**분류**: Core

---

## 1. 목적

플레이어가 자신의 턴에 할 수 있는 행동과 턴 전환 규칙을 정의한다.

---

## 2. 핵심 수치

| 항목 | 값 | 구분 |
| --- | --- | --- |
| 턴 순서 | 게임 시작 시 **무작위** 결정 | 확정 |
| 턴 제한 시간 | 20초 | 확정 |
| 연속 미제출 허용 횟수 | N턴 (N **미정**) | 확정(강제 퇴장) / **미정**(N) |
| 메인 행동 | 턴당 1회 | 확정 |

---

## 3. 메인 행동 (확정)

자신의 턴에 **둘 중 하나만** 선택할 수 있고, 선택하면 턴이 종료된다.

| 선택 | 결과 |
| --- | --- |
| 카드 1장 드로우 | 드로우 완료 후 턴 종료 |
| 행동 카드 1장 사용 | 카드 처리(반응 5초 포함) 완료 후 턴 종료 |

**함정 설치는 메인 행동이 아니다.** 빈 슬롯이 있으면 자신의 턴에 몇 장이든 설치할 수 있고, 설치 후에도 메인 행동을 할 수 있다.

---

## 4. 상태 흐름

| 현재 상태 | 조건 | 다음 상태 | 결과 | 구분 |
| --- | --- | --- | --- | --- |
| `WaitingTurn` | 자신의 차례 도래 | `MyTurn` | 제한 시간 카운트 시작 | 확정 |
| `MyTurn` | 카드 1장 드로우 선택 | `TurnEnd` | 드로우 완료 후 턴 종료 | 확정 |
| `MyTurn` | 행동 카드 1장 사용 | `Resolving` → `TurnEnd` | 체인 처리 완료 후 턴 종료 (불발돼도 종료) | 확정 |
| `MyTurn` | 제한 시간 종료 & 미제출 | `TurnEnd` | 아무것도 하지 않고 턴 종료 (자동 제출 없음) | 확정 |
| `MyTurn` (연속 N턴 미제출) | 위 조건 반복 | 강제 퇴장 | 방에서 퇴장 처리 | 확정(퇴장) / **미정**(N) |
| 임의 상태 | 함정 설치 조건 충족 | 상태 변화 없음 | 함정 슬롯에 배치 | 확정 |
| `TurnEnd` | — | 다음 생존 플레이어의 `WaitingTurn` | 턴 이동 | 확정 |

```
WaitingTurn → MyTurn → (Draw | PlayAction | 시간 초과) → Resolving → TurnEnd → 다음 플레이어
```

---

## 5. 권한

| 처리 | 담당 |
| --- | --- |
| 턴 순서 결정 | 마스터 |
| 현재 턴 주인 저장 | 마스터 → Room CustomProperties |
| 제한 시간 판정 | **마스터** (클라이언트 타이머는 표시 전용) |
| 턴 종료 요청 | 클라이언트 → 마스터 검증 |

상세는 [`09-network.md`](09-network.md).

---

## 6. 턴 순서 결정 (확정 + 제안)

1. 게임 시작 시 마스터가 참가자 목록을 **무작위로 섞어** 턴 순서 배열을 만든다. (확정: 무작위)
2. 이 배열을 모든 클라이언트에 1회 브로드캐스트한다. (제안)
3. 이후 턴 이동은 이 배열에서 다음 **생존** 플레이어를 찾는다. (확정)

> ⚠ 현재 코드(`TurnManager.StartFirstTurn`)는 `PhotonNetwork.PlayerList[0]`을 고정으로 첫 턴에 지정한다. 무작위가 아니다.
> 또한 `GetNextActor`는 `PhotonNetwork.PlayerList`(현재 접속자)를 매번 순회해 순서를 계산하므로 이탈이 발생하면 순서가 흔들린다.

---

## 7. 턴 이동 규칙

| 상황 | 처리 | 구분 |
| --- | --- | --- |
| 정상 종료 | 다음 생존 플레이어로 이동 | 확정 |
| 현재 턴 플레이어가 사망 확정 | 해당 턴 종료 후 다음 생존 플레이어로 | 확정 |
| 다른 플레이어 사망 | 처리 후 기존 턴 플레이어 진행으로 복귀 | 확정 |
| 최종 사망 / 사망 대기 플레이어 | 턴 순서에서 제외 | 확정 |
| 현재 턴 플레이어가 퇴장 | 다음 플레이어에게 턴 이동 | 확정 (구현됨) |
| 턴 스킵 효과 (A01, T05) | 해당 플레이어의 다음 턴 1회 건너뜀 | 확정(카드 존재) / **미정**(중첩 처리) |
| 턴 방향 반전 (A07) | 진행 방향을 반대로 | **미정 — 방향 개념 자체가 정의되지 않음** |

> A07(턴 진행 방향 반전)과 A10(다음 플레이어에게 전달)은 "진행 방향"이라는 개념을 전제한다.
> 현재 기획에는 방향 개념이 없다. **결정 필요.**

---

## 8. 입력

| 입력 | 동작 | 조건 |
| --- | --- | --- |
| 덱 탭 | 드로우 요청 | 자신의 턴 + 메인 행동 미사용 |
| 카드 드래그 → 드롭 영역 | 행동 카드 사용 요청 | 자신의 턴 + 메인 행동 미사용 + 카드 조건 충족 |
| 함정 슬롯으로 드래그 | 함정 설치 요청 | 자신의 턴 + 빈 슬롯 존재 | 
| 턴 종료 버튼 | 턴 종료 요청 | **개발용 디버그 버튼** (정식 흐름 아님) |

> 함정 설치 입력 방식은 **미정**. 현재 코드에 함정 설치 UI가 없다.

---

## 9. UI

| UI | 표시 조건 | 표시 내용 | 구분 |
| --- | --- | --- | --- |
| 제한 시간 | 자신의 턴 | 플레이어 프로필 위 남은 초 | 확정(20초) / **제안**(표시 위치) |
| 자신의 턴 표시 | 자신의 턴 | 프로필 초록색 외곽선 | 확정 |
| 다른 플레이어 턴 | 해당 플레이어 턴 | 해당 좌석 강조 | 확정 |

---

## 10. 예외

| 상황 | 처리 | 구분 |
| --- | --- | --- |
| 게임 종료 / 연결 끊김 | 미제출로 판정 | 확정 |
| 손패가 0장인데 시간 초과 | 일반 시간 초과와 동일 (턴 종료) | 확정 |

---

## 11. 현재 구현 상태

| 항목 | 상태 |
| --- | --- |
| 턴 주인 Room Property 저장/동기화 | 구현됨 (`TurnManager`) |
| 턴 종료 요청 → 마스터 검증 | 구현됨 |
| 이탈 시 턴 이동 | 구현됨 (단, `GetNextActor` 인자 무시 버그 있음) |
| 첫 턴 무작위 결정 | **미구현** (`PlayerList[0]` 고정) |
| 턴 제한 시간 / 타이머 | **미구현** |
| 시간 초과 턴 종료 | **미구현** |
| 연속 미제출 강제 퇴장 | **미구현** |
| 메인 행동 1회 제한 | **미구현** (드로우와 카드 사용을 무제한 반복 가능) |
| 사망 플레이어 턴 제외 | **미구현** |
| 턴 스킵 / 방향 | **미구현** |

---

## 12. 미정 / 결정 필요

1. 연속 미제출 강제 퇴장 기준 N (턴 수)
2. 턴 진행 방향 개념 도입 여부 (A07, A10 카드 전제)
3. 턴 스킵 중첩 시 처리 (2회 스킵이 쌓이면?)
4. 함정 설치 입력 방식

> 2026-09-22 노션 「개발 기획서」(9/18) 반영: 턴 제한 시간 20초 확정, **자동 제출 폐기**(시간 초과 시 턴만 넘김), 연속 미제출은 강제 퇴장으로 확정(N 미정).
