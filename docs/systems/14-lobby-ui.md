# 14. 로비 화면 UI

**최종 수정일**: 2026-09-25
**분류**: MVP
**출처**: 2026-09-25 로비 개편 결정 (플랜 세션). 디자인 시안 없음 — **크기와 배치만 정한다.** 색 · 스프라이트 · 폰트 연출은 디자인이 나오면 별도 문서(또는 이 문서의 개정)로 정한다.

> 방 생성 · 참가의 **규칙**(코드 · 인원 · 실패 처리)은 [`08-room.md`](08-room.md)가 기준이다. 이 문서는 로비 **화면**만 다룬다.

---

## 1. 목적

접속을 마친 플레이어가 방을 만들거나 참가하는 화면. 타이틀([`12-title-ui.md`](12-title-ui.md)) 다음, 방([`08-room.md`](08-room.md)) 이전.

## 2. 판정 주체

로비 화면의 판정은 전부 **로컬**이다. 버튼은 `NetworkManager`에 방 생성 · 참가를 **요청**만 하고, 결과(성공 → 방 씬 이동 / 실패 → 안내)는 Photon 콜백으로 받는다.

## 3. 핵심 규칙

| # | 규칙 | 구분 |
| --- | --- | --- |
| 1 | 화면에는 **지금 동작하는 기능만** 둔다. 기능이 없는 버튼(설정 · 친구 · 프로필 편집 등)은 두지 않는다 — "보이는데 안 눌리는 UI" 금지 | 확정 (2026-09-25) |
| 2 | 로비에서 하는 일은 셋뿐이다: **랜덤 매칭 · 방 만들기 · 방 참가** | 확정 (2026-09-25) |
| 3 | 방 참가 코드 입력과 실패 안내는 **공용 팝업**을 쓴다 (`InputPopup` · `WarningPopup`). 로비 전용 팝업을 만들지 않는다 | 확정 |
| 4 | 로비 화면은 닉네임 외에 플레이어 정보를 표시하지 않는다 (전적 · 친구 코드 · 프로필 이미지는 기획 미확정) | 확정 (2026-09-25) |

## 4. 주요 수치

### 4-1. 캔버스

| 항목 | 값 | 구분 |
| --- | --- | --- |
| 기준 해상도 | 1920 × 1080 | 확정 (타이틀과 동일) |
| 스케일 방식 | Scale With Screen Size · **높이 기준**(Match 1) | 확정 (타이틀과 동일) |
| 화면 방향 고정 | — | **미정** (README 전역 미정 #9) |

> 아래 px 값은 전부 **1080 높이 기준 캔버스 좌표**다. 폰(가로, 화면 높이 ≈ 390pt)에서는 캔버스 1 ≈ 0.36pt.

### 4-2. 크기 기준 (모바일 가독성)

터치 영역 44pt 이상, 글자 11pt 이상 (Apple HIG. 타이틀 4-3절과 같은 기준).

| 요소 | 캔버스 값 | 폰 실제 크기 (390pt 높이 기준) | 구분 |
| --- | --- | --- | --- |
| 메인 버튼 | 480 × 200 | ≈ 173 × 72pt | 확정 (2026-09-25) |
| 메인 버튼 글자 | 56 | ≈ 20pt | 확정 (2026-09-25) |
| 닉네임 글자 | 48 | ≈ 17pt | 확정 (2026-09-25) |

## 5. 입력

| 입력 | 동작 | 조건 |
| --- | --- | --- |
| 랜덤 매칭 탭 | `NetworkManager.JoinRandomRoom()` 요청. 빈 방이 있으면 참가, 없으면 새 방 생성 (08-room 4절) | 항상 |
| 방 만들기 탭 | `NetworkManager.CreateRoom()` 요청 (설정 화면 없이 바로 생성) | 항상 |
| 방 참가 탭 | 방 코드 입력 팝업(`InputPopup`) 열기 | 항상 |
| 코드 입력 팝업 제출 | `NetworkManager.JoinRoom(code)` 요청 | 코드 입력됨 |
| 코드 입력 팝업 닫기 | 팝업 닫기, 로비 유지 | 항상 |

## 6. 화면 상태

| 현재 | 조건 | 다음 | 화면 변화 |
| --- | --- | --- | --- |
| 대기 | 랜덤 매칭 · 방 만들기 · 코드 제출 | 요청 중 | (변화 없음 — 요청 중 표시는 **제안**, 7절 참고) |
| 요청 중 | 방 참가 성공 (`OnJoinedRoom`) | — | `RoomScene`으로 이동 |
| 요청 중 | 생성 · 참가 실패 | 대기 | `WarningPopup`으로 사유 표시 |

## 7. 화면 구성

```
┌────────────────────────────────────────────────────────┐
│ Nickname                                               │
│                                                        │
│                                                        │
│    ┌──────────┐   ┌──────────┐   ┌──────────┐          │
│    │ 랜덤 매칭 │   │ 방 만들기 │   │ 방 참가  │          │
│    └──────────┘   └──────────┘   └──────────┘          │
│                                                        │
│                                                        │
└────────────────────────────────────────────────────────┘
```

| UI | 표시 조건 | 표시 내용 | 동작 | 구분 |
| --- | --- | --- | --- | --- |
| 닉네임 | 항상 | 타이틀에서 정한 닉네임 (`NetworkManager.Nickname`) | — | 확정 |
| 랜덤 매칭 버튼 | 항상 | "랜덤 매칭" | 5절 | 확정 |
| 방 만들기 버튼 | 항상 | "방 만들기" | 5절 | 확정 |
| 방 참가 버튼 | 항상 | "방 참가" | 5절 | 확정 |
| 방 코드 입력 팝업 | 방 참가 탭 | 안내 "방 코드 입력", 제출 "참가" | 5절 | 확정 |
| 실패 안내 팝업 | 생성 · 참가 · 랜덤 매칭 실패 | 제목 + 사유 | 확인 → 닫힘 | 확정 (사유 문구는 08-room 7절) |
| 요청 중 표시 | 요청 후 응답 전 | `LoadingPopup` | 중복 탭 차단 | **제안** — 이번 개편 범위 밖 (`ui-refactoring-plan` PR5) |

### 7-1. 배치

| 요소 | 크기 | 앵커 · 위치 | 구분 |
| --- | --- | --- | --- |
| 닉네임 | 글자 48, 높이 = 글자 줄 높이, 너비 최대 800 (넘치면 말줄임) | 좌상단 앵커. 왼쪽 48 · 위 48 | 확정 (2026-09-25) |
| 메인 버튼 줄 | 3 × (480 × 200), 간격 60 → 총 1560 × 200 | 화면 정중앙 앵커, 가로 한 줄 | 확정 (2026-09-25) |
| 버튼 순서 | 왼쪽부터 랜덤 매칭 · 방 만들기 · 방 참가 | | 확정 (2026-09-25) |

- 총 폭 1560은 16:9(1920) 안에 들어가고, 넓은 폰(19.5:9 → 2340 폭)에서는 좌우 여백이 늘어날 뿐이다.
- ⚠ **4:3 태블릿(iPad)** 에서는 높이 기준 스케일 때문에 캔버스 폭이 1440으로 줄어 **양 끝 버튼이 60씩 잘린다** (2026-09-25 캡처로 확인). Screen Match Mode 를 Expand 로 바꾸면 해결된다 — 전역 미정 #9(화면 방향 · 스케일 방식)와 함께 결정. 결정 전까지 태블릿은 지원 대상이 아니다.
- 가장자리 요소(닉네임)는 모서리에 앵커한다. 화면이 늘어나면 같이 바깥으로 이동한다.
- 높이 기준 스케일이라 세로 배치는 어떤 비율에서도 같다.

### 7-2. 이번 개편에서 두지 않는 것

| 요소 | 사유 | 나중에 어디서 |
| --- | --- | --- |
| 프로필 이미지 · 닉네임 변경 · 전적 · 친구 코드 | 기획 **제안** 단계, 저장할 곳 없음 | 설정 / 프로필 문서 (미작성) |
| 친구 버튼 · 친구 초대 | Optional | [`13-friend.md`](13-friend.md) |
| 설정 버튼 · 사운드 · 메뉴 버튼 | 동작 **미정** (타이틀과 공통) | README 전역 미정 #9 |
| 커스터마이징 · 배너 광고 | Future | — |
| 방 생성 설정 화면 (인원 · 공개) | 설정 항목 **미정** (08-room 5절 #7) | [`08-room.md`](08-room.md) |

## 8. 배경 · 색

**미정** (디자인 없음). 이번 개편은 카메라 단색 배경 + 기본 UI 스프라이트(회색 박스) + 기본 글자색으로 만든다. 디자인이 정해지면 4-2 크기와 7-1 배치는 유지한 채 색 · 스프라이트만 바꾼다.

## 9. 현재 구현 상태

| 항목 | 상태 |
| --- | --- |
| 닉네임 표시 (`ProfilePresenter` · `ProfileView`) | 구현됨 — 씬 연결 완료 (2026-09-25, PR5) |
| 랜덤 매칭 · 방 만들기 · 방 참가 (`LobbyPanel`) | 구현됨 — 씬 연결 완료 (2026-09-25, PR5) |
| 방 코드 입력 팝업 · 실패 안내 팝업 | 구현됨. 실패 사유는 `returnCode` 별 문구 (08-room 7절), 제출 시 입력 팝업 닫힘 |
| 요청 중 표시 | 구현됨 — 요청 직후 `LoadingPopup`, 성공 시 씬 전환이 닫고 실패 시 안내 팝업으로 교체 |
| 캔버스 1920×1080 · 높이 기준 스케일 | 구현됨 (2026-09-25) |
| 7-1 배치 (뷰) | 구현됨 (2026-09-25, 디자인 없이 기본 스프라이트) |

## 10. 미정 / 결정 필요

1. 화면 방향 고정 · Screen Match Mode(높이 기준 vs Expand — 4:3 태블릿 잘림, 7-1 참고) (README 전역 미정 #9)
2. 배경 · 색 · 폰트 연출 (디자인)
3. 사운드 · 메뉴 버튼을 로비에도 두는가 (타이틀 동작이 정해진 뒤)
4. 요청 중 표시를 `LoadingPopup`으로 할지, 버튼 글자 교체("접속 중…"처럼)로 할지
